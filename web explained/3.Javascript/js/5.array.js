let devices = [];
devices = ["Android", "Laptop", "Camera"];
console.log(devices);
//  ["Android", "Laptop", "Camera"]
/* The code above creates an array */
console.log(devices[2]);
// Camera
/* In programing, we count from 0,1,2 not 1,2,3 so 
the code above selects the number 2 item which is mathematically number 3 */
let numbers=[];
numbers=[1,9,2,8,3,7,4,6,5,0]
console.log(numbers[2] + numbers[1]);
//11
/* The code above adds item 2 and item 1 together */
console.log(numbers.sort());
numbers.push(17);
console.log(numbers);
//EXAMPLE
let mix = [];
mix = ['one', 1, 'two', 2];
console.log(mix);